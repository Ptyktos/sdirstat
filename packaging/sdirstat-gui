#!/bin/sh
# Back-compat shim: the GUI launcher is now a built-in subcommand. `sdirstat gui` starts the
# localhost server and opens the GUI as a standalone app window (chromium --app), falling back
# to the default browser.
exec sdirstat gui "$@"
